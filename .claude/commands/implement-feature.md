Implement the following feature: $ARGUMENTS

Follow this workflow strictly:

1. **Research** — Use subagents to investigate relevant files. Understand the current state before changing anything.
2. **Plan** — Think hard and create a plan. Do NOT write code yet. Present the plan for review.
3. **Implement** — After plan approval, make the changes.
4. **Test** — Add or update tests for the changed functionality.
5. **Verify** — Run `pnpm check-types` and `pnpm test`. Fix any issues.
6. **Commit** — Create a commit with conventional commit message (feat: ...).
